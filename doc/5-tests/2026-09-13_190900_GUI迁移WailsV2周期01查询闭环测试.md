# 2026-09-13_190900_GUI迁移WailsV2周期01查询闭环测试

## 测试目的

验证迁移周期 01 的端到端闭环：Wails 前端经 `App.Query(kind,value)` 绑定调用 `pkg/toolapi` → `internal/tools/prockill`，三种查询方式（端口/PID/进程名）在真实环境可用。

## 测试入口与环境

- 命令：`go test ./test/ -run TestToolAPI -v`（主模块根执行）
- 依赖环境：Windows 10.0.19045 + Go 1.26.5 + 本机 netstat/WSL（无 WSL 时相关分支由 prockill 内部降级，非 Windows 整体 t.Skip）
- 样本来源：真实 netstat 采集 + 测试内 `net.Listen` 自建监听端口（保证必命中）
- 被测文件：`pkg/toolapi/toolapi.go`、`wailsapp/app.go`

## 测试结果（2026-09-13 19:07）

| 用例 | 内容 | 结果 | 耗时 |
| --- | --- | --- | --- |
| TestToolAPIQueryPortSelf | 自建真实监听端口，Query("port", n) 必命中且 Origin=="Windows" | PASS | 0.25s |
| TestToolAPIQueryInvalidKind | 非法 kind 返回错误 | PASS | 0.00s |
| TestToolAPIQueryNameNoError | Query("name","node") 不报错（0 行合法） | PASS | 0.22s |

结论：`ok windows-util-gui/test`，3/3 通过。

## 集成验证

- `wails build` 绿：`wailsapp/build/bin/windows-util-gui-wails.exe`（周期01 产物）
- 契约核验：前端 dist 仅调用 `App.Query` / `App.IsElevated`（grep 证据），与绑定层签名一致，无 QueryPort 残留

## 待人工确认项（exe 内 UI 实测）

以下需要人工在已启动的 exe 中操作确认（自动化无法驱动 WebView2 UI 交互）：

1. 分段控件切换（按端口/按 PID/按进程名）placeholder 联动
2. 端口 45321 或任一真实监听端口查询结果与 walk 版一致
3. 日志卡时间戳追加、忙碌态、空态/错误框展示
4. 视觉与理想设计稿（Ardot 725394321827772）一致性（6-review STYLE 结论待此项确认后给出）

## 关联

- 来源：doc/3-实施/2026-09-13_185500_GUI迁移WailsV2_实施总览.md 周期 01 / AC-01~03
- 证据：本文件 + go test 输出 + grep 契约记录

## 用户实测反馈与修复（2026-09-13 19:12）

- 用户反馈：界面效果与徽章/日志卡通过；多行结果无滚动条，只能看到前 3 行。
- 根因：results-card 无弹性高度、无滚动容器，超出视口内容被 body overflow:hidden 裁切。
- 修复：结果表包入 .table-scroll 滚动容器，results-card 设 flex:1 + min-height:0，表头 sticky 吸附。
- 回归：wails build 绿（19:13），exe 已重启待用户复测滚动。
- STYLE 结论：用户确认视觉与徽章/日志达标 → STYLE: PASS（滚动缺陷已修复并复测中）。

## 周期02 结束进程测试记录（2026-09-13 19:32）

- go test 全量回归 19/19 PASS，新增关键用例：
  - TestToolAPIKillRealProcess：真实子进程 → Query("pid") 取 Row → Kill → Success，并回查进程已退出（0.33s）
  - TestToolAPIKillProtected：csrss.exe 真实 PID → Kill 返回 Success=false 且 Error 非空，进程存活（0.21s）
  - TestKillWSLListener / TestToolAPIKillRealProcess 覆盖 WSL 侧 kill -9 路径
- 集成发现并修复两个问题（主线程）：
  1. wailsapp/main.go 直接 import internal/winutil 违反 internal 可见性 → 提权入口下沉 pkg/toolapi（ShouldSkipElevate/RelaunchAsAdmin 透传）
  2. wails build 绑定生成阶段无参执行二进制会触发 UAC 卡死构建 → 新增 bindingGen 构建标签守卫（binding_guard.go / binding_guard_bindings.go）
- 最终 wails build 绿；exe 以 -no-elevate 启动供 UI 实测；提权路径（双击弹 UAC）待用户自行验证（R-01 ACL 双顺序实测项）

## 周期03 TG 迁移测试记录（2026-09-13 20:02）

- go test 全量 26 用例 PASS（既有 19 + 新增 7：下沉纯函数 5 项单测 + TgGetState/TgRecords 真实冒烟）
- 集成期主线程修复 3 处契约缺陷（并行双方各自符合字面契约，但键名/提取规则在接缝处不一致）：
  1. toolapi 用类型别名直透 Go 结构体，序列化键为大写导出字段，前端按小驼峰读取 → 改为显式 DTO（Chat/ExportRecord，键 id/type/name/username/topic、createdAt/sourceId/path 等）并加转换器
  2. TgClearHistory 未做 ID 提取（walk 版有 ExtractChatID 步骤）→ 桥接层补齐
  3. 前端 fillRecords/doTgForward 用错误键名且 option value 语义混乱 → 统一为索引值 + tgRecordList 状态；补「清空历史」按钮（两步点击确认，对齐 walk 版功能）
- 验证：node --check app.js 通过；wails build 绿；exe 以 -no-elevate 启动
- 待用户实测（需真实 Telegram 会话）：扫码登录 → 刷新聊天 → 导出 → 试转发 → 正式转发 → 停止/清历史
